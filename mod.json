{
  "schemaVersion": 1,
  "id": "poly-to-track",
  "name": "Poly To Track",
  "version": "0.9.1",
  "description": "Import 3D files (STL/OBJ) — colored models supported — and build them as PolyTrack block builds: position a viewport ghost with Blender-style 3D transform handles (move/rotate any angle/per-axis scale), then apply. Schematica for PolyTrack.",
  "entrypoint": "dist/entrypoint.js",
  "icon": "assets/icon.svg",
  "mixins": [{ "config": "mixins.json", "environment": "web" }],
  "targets": [">=0.6.0 <0.7.0"],
  "authors": ["roowus"],
  "license": "MIT",
  "homepage": "https://github.com/roowus/poly-to-track",
  "capabilities": ["dom", "storage"],
  "vanillaSafe": true
}
